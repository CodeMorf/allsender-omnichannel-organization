{
  "name": "@allsender/omnichannel-organization",
  "version": "0.1.0",
  "private": false,
  "description": "Workspace-scoped departments, areas, human routing and organization contracts for AllSender Omnichannel.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check backend/index.js && node --check backend/services/organization.service.js && node --check backend/routes/organization.routes.js && node --check backend/integration/register.js",
    "typecheck": "tsc --noEmit",
    "validate": "npm run check && npm run typecheck && npm test"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.18.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
